<UserControl x:Class="ElectronicQueue.AdminClient.View.ServiceManageView"             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectronicQueue.AdminClient.View"
             xmlns:viewmodel="clr-namespace:ElectronicQueue.AdminClient.ViewModel" 
             d:DataContext="{d:DesignInstance Type=viewmodel:ServiceManageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" Background="White">
    <UserControl.Resources>
        <Style x:Key="MenuSectionHederTextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10 5 10 2"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Menu Grid.ColumnSpan ="2">
            <MenuItem Header="Сохранить"/>
            <MenuItem Header="Обновить"/>
        </Menu>
        
        <StackPanel Margin="3" Grid.Row="1">            
            <TextBlock Text="Параметры роли" 
                       Style="{StaticResource MenuSectionHederTextBlock}"/>

            <StackPanel>
                <TextBlock Text="Имя: " Grid.Column="0"/>
                <TextBox Text="{Binding SelectedServiceProvider.Name}" Grid.Column="1"/>
            </StackPanel>

            <TextBlock Text="Списокк ролей"
                       Style="{StaticResource MenuSectionHederTextBlock}"/>
            <ListView ItemsSource="{Binding ServiceProviders}" 
                      SelectedItem="{Binding SelectedServiceProvider}"/>
        </StackPanel>
        
        <DataGrid  HorizontalAlignment="Stretch" 
                   ItemsSource="{Binding SelectedServiceProvider }"
                   Grid.Row="1" Grid.Column ="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование услуги" />
                <DataGridComboBoxColumn Header="Предосталяющая услугу роль" />
                <DataGridCheckBoxColumn Header="Услуга предоставляется" IsThreeState="False"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</UserControl>
